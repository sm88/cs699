\documentclass[12pt]{article}
\pagestyle{plain}
\begin{document}

\begin{verbatim}
\documentclass[<font-size>pt]{<document-class>}
\end{verbatim}
This command sets the style of the document and takes an optional font-size parameter. This should be the first command in the document.

\begin{verbatim}
\begin{document}
\end{verbatim}
Marks the beginning of the body of the document

\begin{verbatim}
\end{document}
\end{verbatim}
Marks the end of the document. Can be used for error checking as latex will not process the document after reading the end tag, so it can be inserted anywhere in the middle to figure out where an error might have been.

\end{document}
